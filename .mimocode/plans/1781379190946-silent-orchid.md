# Plan: Dopisat' Terakan do Vulkan 1.1

## Context

Terakan — Vulkan driver for AMD TeraScale (R600–NI) inside Mesa. Vulkan 1.1 API version, not conformant. Patch queue on `Terakan_state_rework` (upstream `a5fc39658`): 0001–0004, 0010.

**Upstream clone:** `/home/shipa/terakan-mesa-state-rework`
**Patch queue:** `/home/shipa/Projects/mesa-terakan-mimo-development/patches/`

**Goal:** Fill P2 gaps for Vulkan 1.1 conformance; fix P0 3D rendering for multi-binding vertex formats.

---

## Phase 0: Vertex fetch multi-binding — CURRENT (P0)

**Symptoms:**
- vkQuake3: wall textures smeared as colored stripes
- STK: wheels collapsed to center, textures torn; 2D/menu OK
- STK uses 4 bindings: position/16, color/4, texcoord/8, lightmap/8

**Debug so far:**
- Strides 16/4/8/8 look correct
- `res_used=0x7/0xf` — resources appear bound
- `apply_sq_resources_fetch` unbind limited to `< MUTABLE_BASE` (0010)
- Dynamic VI stride propagation added (0010)

**Next steps:**
1. Trace VTX fetch shader instructions vs r600g for multi-buffer vertex input
2. Verify vertex attribute format encoding in hardware state
3. Compare sampler/texture state for 3D vs working 2D paths
4. IB dump on Palm during STK draw vs vkcube

**Files likely involved:**
- `terakan_app_config_draw.c` — `apply_sq_resources_fetch`
- `terakan_vertex_input.c`, `terakan_vk_state.c`
- SFN vertex fetch / `sfn_shader.cpp`

**Verification:** vkQuake3 q3dm1 + STK in-game 3D look correct on Palm

---

## Phase 1: Fix Compute MVP — DONE ✅

`terakan-test-compute` PASS `{1,2,3,4}` on Palm.

Fixes in patch **0010:**
- CB UAV guard when compute active
- CS barriers before/after dispatch
- SFN kcache 16→15, push constants `base_vertex`/`base_instance`
- Stride/VI propagation, `remaining_stages` loop fix

---

## Phase 2: CmdDrawIndirectCount

**Why:** Required for DXVK/VKD3D and some engines. Indirect draw already works (patch 0004).

**Implementation:**
1. Add `terakan_CmdDrawIndirectCount` and `terakan_CmdDrawIndexedIndirectCount` in `terakan_vk_draw.c`
2. Read `drawCount` from buffer offset via CPU map (same pattern as `CmdDispatchIndirect`)
3. Loop `PKT3_DRAW_INDIRECT` / `PKT3_DRAW_INDEX_INDIRECT`

**Files:** extend **0004** or new **0005-draw-indirect-count.patch**

---

## Phase 3: VK_KHR_maintenance3 + GetDescriptorSetLayoutSupport

Advertise extension; implement `vkGetDescriptorSetLayoutSupport` → `supported = VK_TRUE`.

---

## Phase 4: Pipeline Cache

Serialize pipeline state; `CreatePipelineCache` / `GetPipelineCacheData`; UUID in `terakan_device.c`.

---

## Phase 5: CmdClearDepthStencilImage

Meta shader path similar to color clear.

---

## Phase 6: CmdResolveImage / MSAA resolve in CmdEndRendering

Reuse meta copy pattern. Phase 7 (EndRendering resolve) deferred until core solid.

---

## Execution Order

1. **Phase 0** (vertex fetch) — **current P0**
2. **Phase 2** (DrawIndirectCount)
3. **Phase 3** (maintenance3)
4. **Phase 4** (pipeline cache)
5. **Phase 5–6** (depth clear, resolve)

---

## Verification After Each Phase

1. `meson compile -C build-vulkan` — must succeed
2. Deploy to Palm: see [AGENTS.md](../AGENTS.md) §4
3. Smoke: `vkcube`, `vkgears`, `vulkaninfo`, `terakan-test-capabilities`
4. Phase 0: vkQuake3 + STK 3D visual check
5. Eventually: Khronos VK-GL-CTS 1.1 smoke caselist
